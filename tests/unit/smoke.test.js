import { describe, expect, it } from 'vitest';

// Placeholder so CI has a test to run; replaced by levels/calibration tests in milestone 3.
describe('smoke', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2);
  });
});
