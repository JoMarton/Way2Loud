// @ts-check

/** Entry point. Wires screens: Start → Calibrate → Listen (added in later milestones). */

const statusEl = /** @type {HTMLElement} */ (document.getElementById('status'));
statusEl.textContent = 'Hello! The meter is coming soon.';
